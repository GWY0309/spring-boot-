<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.badmintonsystem.mapper.UserMapper">

    <select id="findByUsername" resultType="com.example.badmintonsystem.entity.User">
        SELECT
            id, username, password, nickname, phone, role, create_time, update_time
        FROM
            t_user
        WHERE
            username = #{username}
    </select>

    <select id="findAll" resultType="com.example.badmintonsystem.entity.User">
        SELECT id, username, nickname, phone, role, create_time, update_time FROM t_user
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, nickname, phone, role, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{phone}, #{role}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE t_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="password != null">password = #{password},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.example.badmintonsystem.entity.User">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

</mapper>