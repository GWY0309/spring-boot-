<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.badmintonsystem.mapper.RacketRentalMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_racket_rental (racket_id, user_id, rental_time, status)
        VALUES (#{racketId}, #{userId}, NOW(), 0)
    </insert>

    <select id="findByUserId" resultType="com.example.badmintonsystem.entity.RacketRental">
        SELECT *
        FROM t_racket_rental
        WHERE user_id = #{userId}
        ORDER BY rental_time DESC
    </select>

    <select id="findById" resultType="com.example.badmintonsystem.entity.RacketRental">
        SELECT * FROM t_racket_rental WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE t_racket_rental
        SET
            return_time = #{returnTime},
            total_cost = #{totalCost},
            status = #{status}
        WHERE
            id = #{id}
    </update>

    <select id="findAll" resultType="com.example.badmintonsystem.entity.RacketRental">
        SELECT *
        FROM t_racket_rental
        ORDER BY rental_time DESC
    </select>

</mapper>